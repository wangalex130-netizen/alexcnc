# alexcnc · 三任务一致性终稿（v2.0 终版）

> **地位**：以 APP 任务为修改源头，三方同步后作为唯一权威契约。  
> **阅读对象**：APP（Flutter）/ 固件（ESP32-S3 + GRBL）/ MQTT 服务器 / 部署 四方。  
> **变更来源**：合并 APP 任务 `consistency-review.html`、MQTT 任务 `docs/13-three-task-summary.md`、固件侧 11 项拍板结论后的统一版本。  
> **最终拍板**：固件侧在 2026-08-13 已明确「11 项拍板，以我为准，三方照此对齐」，本终稿以该拍板为基准。

---

## 0. 一句话流程

```
App UI ──调用──▶ HardwareService / CloudService（抽象接口）
                        │
        三方实现 ────────┤  RealHardwareService  （App ↔ 机器：LAN TCP:8899 / WAN MQTT）
                        └  RealCloudService     （App ↔ 云端 REST）
```

机器 = **ESP32-S3 屏幕（MQTT clientId = `screen-{deviceId}`）+ 板载 GRBL（UART 直控）**。无 STM32F407 主控；如未来 407 独立联网，仅作为 `bridge-{deviceId}` 预留身份。

---

## 1. 三大设计原则

1. **机器是唯一真值源（SSOT）**  
   App 不裁决坐标、刀仓、状态；只监听机器广播的状态并渲染。所有 `MachineStatus` 由 MCU 产生。

2. **资产闭环：手机绝不持有真实 G-code**  
   切片文件由云端经局域网 / MQTT **直接下推给 MCU**。App 只取任务元数据 `TaskMetadata` 和极小的 2D 矢量渲染 JSON。App 永不下载 / 存储实体 G-code。

3. **LAN / WAN 鉴权**  
   手机与机器同 Wi-Fi → `isLocalLAN = true`（全功能：Jog、回零、开切）。  
   手机走 4G/5G 公网 / 云端 MQTT → `isLocalLAN = false`（监视模式：锁死 Jog / 开切，仅留 监控 + pause/resume/stop + light/fan + OTA）。

---

## 2. 11 项统一拍板

| # | 项目 | 终版拍板 | 历史旧表述 |
|---|---|---|---|
| 1 | 局域网直连端口 | **TCP:8899** | TCP:8080（MQTT 设计笔误） |
| 2 | MQTT 主题命名空间 | `cnc/<deviceId>/{cmd,status,notify,telemetry,log}` + `cnc/broadcast/#` + `sys/#` | 旧全局 `grbl/command`、`grbl/status`、`grbl/log` |
| 3 | MQTT 端口/加密 | 生产 **8883 TLS** + 关匿名 + 私有 broker；**1883 仅本地联调** | APP/固件文档写 1883 明文 |
| 4 | 帧格式 | **APP 扁平 JSON 为线协议真值**；`src/dst/seq/v/ts` 由 MQTT 层承载（topic 结构 + 5.0 user properties） | 曾讨论 envelope 封套 / 全扁平 |
| 5 | 自检阶段数 | **scTotal = 5**（固件源码真值） | APP 文档曾写 8 |
| 6 | 状态机 | **APP 枚举为协议标准**：`disconnected / idle / homing / busy / paused / alarm`；`ready/awaitingConfirm` 是 flag 不是状态；自检用 `busy + scIndex/scTotal` 表达 | 固件曾用 phase 0-5 / broker 曾用 selfcheck/run |
| 7 | Jog 参数 | `{"cmd":"jog","axis":"x","dist":±1.0}` 单轴带符号（APP 格式） | 曾讨论 x+y+feed 多轴同发 |
| 8 | 主控身份 | **ESP32-S3 屏幕（screen-*，MQTT 唯一客户端）+ 板载 GRBL（UART 管辖）**；无 STM32F407；407 若独立联网作为 `bridge-*` 预留 | 曾写 STM32F407 主控 |
| 9 | RTSP 端口 | **:554**（APP 真机实测） | MQTT 设计写 :81 |
| 10 | WAN 白名单 | 统一为：**监控 + pause/resume/stop + light/fan + OTA**；禁 jog / 启动 | APP 曾仅 stop/pause |
| 11 | 设备注册 | **互补**：`hello` 命令帧（连接层，`deviceId == serial` 唯一码）+ `sys/#`（broker ACL） | 曾各自独立 |

---

## 3. 传输层

### 3.1 局域网（LAN）—— TCP:8899 直连

- **端口固定 `8899`**。机器侧开 TCP Server（`0.0.0.0`），App 作为 TCP Client 直连 `机器IP:8899`，长连接，双向收发。
- 报文：**换行分隔的 JSON 帧**（每条以 `\n` 结尾）。命令/状态走同一连接。
- 断线后 App 每 5s 重连；`disconnect()` 主动关闭不再重连。
- 不依赖任何 Broker / 云端 / 外网。
- 机器联网后广播 mDNS：`alexcnc-<sn>.local`（Service：`_alexcnc._tcp.local`，port **8899**）。
- 参考实现：`server/fake_firmware.py --tcp`。

### 3.2 外网（WAN）—— MQTT Broker

- **主链路**：云端 MQTT Broker（生产环境）。
  - 生产端口：**8883 TLS**，关匿名，私有 Broker 域名。
  - 本地/开发联调端口：**1883**（仅限内网/本机，不上生产）。
  - WebSocket 端口统一为 **8083**（避免 EMQX 8083 vs Mosquitto 9001 混用；Mosquitto 部署需显式改成 8083 或文档注明）。
- **主题命名空间（每设备隔离）**：

| 主题 | 方向 | QoS | Retain | 说明 |
|------|------|-----|--------|------|
| `cnc/<deviceId>/cmd` | App/PC → 机器 | 1 | 否 | 命令下发（唯一下行通道） |
| `cnc/<deviceId>/status` | 机器 → App/PC | 1 | **是** | 权威状态，新订阅者立即可见 |
| `cnc/<deviceId>/notify` | 机器 → App/PC | 1 | 否 | 事件通知：job_done / alarm / tool_changed / confirm_required 等 |
| `cnc/<deviceId>/telemetry` | 机器 → App/PC | 0 | 否 | 高频遥测（坐标/速度，10Hz），可选 |
| `cnc/<deviceId>/log` | 机器 → App/PC | 0 | 否 | 调试日志，默认关闭 |
| `cnc/broadcast/#` | 机器/云端 → 全部 | 1 | 否 | 全局广播 |
| `sys/#` | admin / 云端 | 1 | 视场景 | 注册 / ACL 同步（仅 admin） |

- **App 至少订阅**：`cnc/<deviceId>/status` + `cnc/<deviceId>/notify`（否则收不到 job_done / alarm）。`telemetry` / `log` 可选。
- **clientId 规则**：
  - 手机 App：`app-<userId>`
  - 机器屏幕：`screen-<deviceId>`
  - 电脑端：`pc-<userId>`
  - 407 独立联网（预留）：`bridge-<deviceId>`
  - 设备运维：`admin`
- **ACL 原则**：
  - `app-*` 只能向自己绑定的 `cnc/{deviceId}/cmd` 发布，只能订阅自己绑定设备的 `status/notify/telemetry/log` 和 `cnc/broadcast/#`。
  - `screen-{id}` 只能发布 `cnc/{id}/status|notify|telemetry|log`，只能订阅 `cnc/{id}/cmd` 和 `cnc/broadcast/#`。
  - 多租户按 `deviceId` 隔离；任何终端无法伪造他人状态。

### 3.3 报文格式：APP 扁平 JSON 为线协议真值

**线协议（TCP / MQTT payload 统一）**：

```json
// 命令示例
{"cmd":"jog","axis":"x","dist":1.0}
{"cmd":"job","action":"start"}
{"cmd":"hello","model":"LY_3020_2.0","serial":"<唯一码>","proto":1}

// 状态示例
{"state":"busy","pos":{"x":1.0,"y":0.5,"z":-1.2},"scIndex":3,"scTotal":5,...}
```

**MQTT 层承载路由元数据**：
- `src` / `dst` / `seq` / `v` / `ts` 不放在 payload，而由 MQTT 层提供：
  - `dst` 由 **topic 名** `cnc/<deviceId>/cmd` 中的 `deviceId` 决定；
  - `src` 由 **clientId**（如 `app-u123`、`screen-{id}`）决定；
  - `seq` / `v` / `ts` 使用 **MQTT 5.0 user properties**（或 LWT/消息属性）。
- broker 内部节点互连可继续使用封套，但**进出 APP 与固件的数据载荷必须是 APP 扁平 JSON**。

**设计理由**：
- APP 业务/UI/模型零改动；
- 固件 MQTT 解析成本最低（直接解析扁平 JSON）；
- 路由、版本、防重放由 MQTT 层负责，不污染业务 payload。

### 3.4 命令映射表

| App 方法 | 线协议 JSON | 固件 / 主控动作 |
|---|---|---|
| `jog(axis, distMm)` | `{"cmd":"jog","axis":"x","dist":1.0}` | `G91 G0 X1.0` 或 Grbl 实时 `$J=G91 X1 Fxxx`；`dist` 带正负号 |
| `home()` | `{"cmd":"home"}` | `$H` 回机械原点（带锁机保护） |
| `setWorkZero(x,y,z)` | `{"cmd":"setWorkZero","x":0,"y":0,"z":0}` | `G10 L20 P1 X0 Y0 Z0` |
| `startSpindle(rpm)` | `{"cmd":"spindle","rpm":12000}` | `M3 S12000`；`rpm=0` → `M5` |
| `stopSpindle()` | `{"cmd":"spindle","rpm":0}` | `M5` |
| `setAux(key, on)` | `{"cmd":"aux","key":"light","on":true}` | `key` ∈ `light` / `laser` / `timelapse` / `fan`；自定义 `$` 或 M-code |
| `startJob()` | `{"cmd":"job","action":"start"}` | 触发机器执行已落盘的 G-code |
| `pauseJob()` | `{"cmd":"job","action":"pause"}` | 软暂停（保留坐标） |
| `resumeJob()` | `{"cmd":"job","action":"resume"}` | 继续 |
| `stopJob()` | `{"cmd":"job","action":"stop"}` | 软停止（抬刀 / 回安全位） |
| `confirm()` | `{"cmd":"confirm"}` | **仅供联调/演示**；正式形态为机身屏物理按钮，量产固件可忽略 |
| `updateToolMap(tools)` | `{"cmd":"toolMap","tools":[{"index":1,"installed":true},...]}` | 下发 ATC 刀仓映射；固件四刀位传感器只校验在位 |
| `setLevelingPlan(mode, cols, rows)` | `{"cmd":"leveling","mode":1,"cols":5,"rows":4}` | 下发调平网格方案；cols/rows 由 App 按云端模型尺寸算好 |
| `hello` | `{"cmd":"hello","model":"...","serial":"...","proto":1}` | 连接建立后上报机型与唯一码 |

### 3.5 `notify` 事件格式（机器 → App）

App 必须订阅 `cnc/<deviceId>/notify`：

```json
{
  "type": "job_done",
  "taskId": "task-001",
  "msg": "加工完成"
}
```

常见 `type`：`job_done` / `alarm` / `error` / `tool_changed` / `confirm_required`（D9 等待机旁确认提示）。App 消费时未知 type 兜底显示 `msg`。

---

## 4. 状态广播 `statusStream`（机器 → App）

线协议 JSON Schema：

```json
{
  "state": "idle",
  "pos":  { "x": 12.34, "y": 5.60, "z": -2.10 },
  "mpos": { "x": 12.34, "y": 5.60, "z": -2.10 },
  "rpm":  12000,
  "feed": 600,
  "progress": 0.42,
  "etaSec": 180,
  "msg": "ok",
  "scIndex": 3,
  "scTotal": 5,
  "download": 0.6,
  "awaitingConfirm": false,
  "aux": { "light": true, "laser": false, "timelapse": false, "fan": false },
  "tools": [
    { "index": 1, "name": "3.175平底刀", "material": "钨钢", "length": 30.0, "installed": true },
    { "index": 2, "name": "1.5球刀",    "material": "钨钢", "length": 22.0, "installed": true },
    { "index": 3, "name": "—", "installed": false },
    { "index": 4, "name": "—", "installed": false }
  ]
}
```

字段说明：
- `state`：`idle` · `homing` · `busy` · `paused` · `alarm` · `disconnected`。
- `pos`：G54 工作坐标 (mm)。
- `mpos` / 别名 `mp`：机器坐标 (mm)。
- `rpm` / 别名 `spindle`：主轴转速 rpm；关闭时为 null。
- `feed`：mm/min；无进给时为 null。
- `progress` / 别名 `prog`：加工进度 0..1。
- `etaSec` / 别名 `eta`：预计剩余秒；无任务为 null。
- `scIndex` / `scTotal`：自检阶段进度（固件拥有）。**统一为 5 阶段**（见 §5.1）。
- `download`：G-code 文件下载进度 0..1；无下载任务为 null。
- `awaitingConfirm`：D9 是否正等待机旁物理确认。
- `aux`：增加 `fan` 字段；云端 WAN 可操作 light / fan / OTA。
- `tools`：ATC 刀仓（4 槽）。

---

## 5. 状态机 `MachineState`

枚举值（三方统一）：

`disconnected` · `idle` · `homing` · `busy` · `paused` · `alarm`

合法迁移：

```
idle ──home()──▶ homing ──▶ idle
idle ──job prepare──▶ download(progress) ──▶ ready ──startJob──▶ awaitingConfirm(标志位) ──物理确认──▶ busy
idle ──startJob──▶ busy ──pauseJob()──▶ paused ──resumeJob()──▶ busy
busy / paused ──stopJob()──▶ idle
busy ──(异常)──▶ alarm ──(复位)──▶ idle
任意 ──disconnect()──▶ disconnected
```

- `ready` 与 `awaitingConfirm` 是**标志位 / 子状态**，不是独立枚举。
- **自检不是独立态**：`busy` 状态下通过 `scIndex/scTotal` 表达进度。

### 5.1 自检流水线（固件拥有）

- `scTotal` 固定为 **5**，对应固件真实源码的 5 步：
  1. `pp_matched`（配对/门限检查）
  2. `pp_lev`（调平结果确认）
  3. `pp_tool`（刀仓/刀具确认）
  4. `pp_warm`（主轴预热）
  5. `pp_all`（全部就绪）
- 时序参考（固件当前实现）：`[1100, 3300, 1600, 1800, 900]` ms，合计约 8.7s。
- App **只渲染**，不自己计时；`scIndex >= scTotal` 后进入加工态（`progress` / `etaSec`）。

---

## 6. G-code 下发链路

**不变式**：App 永不持有 G-code。真实切片文件由云端经 LAN / MQTT **直推 MCU**。

标准流程：

```
① 电脑端/云端把 G-code 存到文件服务 → 得到可下载 URL（预签名）
② 下发 job prepare：{"cmd":"job","action":"prepare","gcodeUrl":"http://.../gcode/task-001","compensation":"firmware"}
③ 机器 HTTP 异步下载 → 广播 download 进度（0..1）→ 完成后 state=ready
④ 用户点 startJob → 机器广播 awaitingConfirm=true → 机身屏弹「确认加工」
⑤ 机旁物理按钮确认后，固件执行「自检(sc 0→5) → 加工(progress/eta)」，完成后 state=idle
```

- `compensation` 是**任务属性声明**（`host` 或 `firmware`），每次任务只有一个补偿方，防止重复叠加。
- `gcode` 帧保留为兼容/调试通道（小文件）：`{"cmd":"gcode","lines":[...],"compensation":"..."}`，机器收到后同样落盘存储，再走 ④⑤ 流程。
- 下载失败/校验失败：广播 `state=alarm` + `msg` 原因，可重发 `job prepare` 重试。

---

## 7. 设备唯一码与注册

- `deviceId == serial == 机器唯一码`，三文档统一使用该值作为 MQTT 主题隔离与账号绑定依据。
- 连接建立后，机器主动发 `hello` 命令帧上报机型与唯一码。
- `sys/register` 是 broker 层 ACL 同步机制（admin/云端），与 `hello` 命令帧**互补互引**：
  - `hello` = 连接层身份声明；
  - `sys/register` = broker 层权限/ACL 同步。
- 绑定流：用户注册个人账户 → 手机/电脑输入或扫描机器唯一码 → 云端建立「账号 ↔ 机器」绑定 → 之后按唯一码进入该机器管理。一账号可挂多台，当前**单机连接**（切换即断开上一台）。

---

## 8. LAN / WAN 鉴权与白名单

`RealHardwareService.connect()` 判定：
- 手机与机器在**同一子网**（或连的是本地 MQTT broker）→ `isLocalLAN = true`（全功能）。
- 走公网 / 云端 MQTT → `false`（监视模式）。

**外网（WAN）允许的操作**：
- 监控（状态/遥测/日志查看）
- `pauseJob()` / `resumeJob()` / `stopJob()`
- `setAux(key, on)`：`light` / `fan`
- OTA 触发（白名单）

**外网禁止**：Jog、startJob、home、setWorkZero、spindle 起转等涉及运动/起转的操作。

---

## 9. 摄像头 RTSP

- 局域网 RTSP 直连端口以真机实测为准：**`rtsp://admin:abc123456@<ip>:554/11`**。
- 视频面不经 MQTT；本地预览 + 推腾讯云视频面。

---

## 10. 三方交付清单

### APP 侧（alexcnc）

- [ ] 订阅 `cnc/<deviceId>/status` + `cnc/<deviceId>/notify`；`telemetry` / `log` 可选。
- [ ] WAN 模式白名单补 `light` / `fan` / OTA；运动类命令在 WAN 下由 APP 层拒绝。
- [ ] `setAux` 支持 `fan`；`aux` 状态字段同步含 `fan`。
- [ ] `MachineStatus` / 文档中 `scTotal` 默认值/示例改为 **5**。
- [ ] `config.dart` / 文档：1883 仅联调，生产 8883 TLS + 关匿名。
- [ ] 线协议保持 APP 扁平 JSON（当前已实现）；无需 envelope 映射。
- [ ] 更新 `docs/PROTOCOL.md` 引用到本统一契约。
- [ ] 产出 `contract/views/app_view.json` 供三方自动对账。
- [x] 刀库目录 `info.color` 防呆色：5 把官方刀对齐系统表精确 hex（`tool_library.dart` 已落地 `colorHex`，红 `#D32F2F`/橙 `#E65100`/黄 `#FDD835`/绿 `#43A047`/蓝 `#1E88E5`）。
- [x] MQTT 联调 TLS 自签证书：`_connectMqtt` 已加 `client.secure=true` + `onBadCertificate`（仅联调期，联调 broker `43.154.192.242:8883`，对接 `cnc-control-server/docs/16-app-integration.md`）。**上线换正式 CA（Let's Encrypt）后删 `onBadCertificate` 行**。

### MQTT 服务器侧（cnc-control-server）

- [ ] 文档/设计稿中 `8080` → `8899`。
- [ ] RTSP `:81` → `:554`。
- [ ] Mosquitto 部署补齐 `mosquitto.conf` / `acl.conf` / `passwd`，避免默认开放配置与 EMQX 安全模型不一致。
- [ ] WebSocket 端口统一（推荐统一为 8083，或在文档中明确 EMQX/Mosquitto 差异）。
- [ ] 提供 `notify` / `telemetry` / `log` 的 payload schema 给 App 侧确认。
- [ ] broker 内部互连可保留封套，但**出 broker 到 APP/固件的载荷必须是 APP 扁平 JSON**；`src/dst/seq/v/ts` 通过 MQTT topic + 5.0 user properties 承载。
- [ ] `docs/03-frame-contract.md` 状态机图 + `sim/screen_sim.py` state 字符串 + `verify/run_loopback.py` 断言向 APP 枚举对齐。

### 固件侧（cnc-fw）

- [ ] **T0（致命）**：`main.c` WiFi 连上后调用 `mqtt_client_start()`，真正启动 MQTT 客户端。
- [ ] **T1**：`MQTT_BROKER_URI_DEFAULT` 指向部署的私有 broker（内网 `:1883` 或云端 `:8883` + 证书），不再默认 `broker.emqx.io`。
- [ ] **T1**：MQTT 主题从 `grbl/command|status|log` 改为 `cnc/<deviceId>/cmd|status|notify|telemetry|log`，并订阅 `cnc/<deviceId>/cmd` + `cnc/broadcast/#`。
- [ ] **T1**：身份 `screen-<deviceId>` + 密码；连云端走 TLS。
- [ ] **T1**：`deviceId` / broker / 凭据存 NVS，可配置化，不写死。
- [ ] **T2**：LAN TCP Server 按 **8899** 实现，解析 §3.4 命令帧，广播 §4 状态帧（含 `scIndex/scTotal=5`）。
- [ ] **T3**：心跳超时自动 Feed Hold（按 docs/08 T3）。
- [ ] **T4**：UDP 45454 `CNC-SCREEN` beacon（按 docs/08 T4）。
- [ ] 订阅 `cnc/<deviceId>/tool_catalog`；机身屏展示刀具时带 `info.color` 防呆色（与 App 定位环色一致：红/橙/黄/绿/蓝精确 hex）。
- [ ] 文档措辞：删除 "STM32F407 主控"，改为 "ESP32-S3 屏幕 + 板载 GRBL（UART 直控）"；407 仅作为 `bridge-*` 预留。

---

## 11. 验证机制

- MQTT 任务侧已提供 `verify/contract_check.py`，任何人（APP/固件/服务器）跑一次即可自动列出差异。
- 本终稿配套 `contract/views/app_view.json`（见 APP 交付清单），让脚本可自动对账。
- 三方各自改完后，各自跑 `verify/contract_check.py`，全绿即对齐。

---

## 12. 历史版本对照

| 旧表述 | 统一后表述 |
|---|---|
| 局域网 `TCP:8080` | `TCP:8899` |
| MQTT 全局 `grbl/command` | `cnc/<deviceId>/cmd` |
| 自检 8 阶段 | 自检 5 阶段 |
| 状态 `run` / `selfcheck` | `busy` + `scIndex/scTotal` |
| 摄像头 RTSP `:81` | `:554` |
| 命令 envelope 封套上 wire | APP 扁平 JSON 为线协议真值；元数据走 MQTT 层 |
| STM32F407 主控 | ESP32-S3 屏幕 + 板载 GRBL（UART）；407 作为 `bridge-*` 预留 |

---

## 16. 部署拓扑与跨云集成（工程师 2026-08-13 补充，详见 `多端架构与规则契约总览.html` v2.1）

- **双云**：视频流服务 + MQTT Broker = **腾讯云同一台**（MQTT 主链路 8883）；三端 REST API + DB = **阿里云 037123.xyz 另一台**（设备绑定 / 刀库更新 / 模型库 / 任务 / G-code 文件服务）。
- App 与固件均跨云双连：App → 阿里云(REST) + 腾讯云(MQTT/视频)；固件 → 腾讯云(MQTT) + 阿里云(G-code 下载)。
- **关键新增集成点**：阿里云 API 服务器必须也是腾讯云 broker 的 MQTT 客户端（admin/bridge 身份），用于① 设备绑定后跨云同步 broker ACL（`sys/#`）；② 服务端发起的 job prepare 等命令下发（`cnc/<deviceId>/cmd`）。否则绑定写了 DB、broker 仍无权限。
- G-code 文件服务建议与阿里云 API 同云，固件 HTTP 下载；视频流在腾讯云。

## 17. 现有 API 业务接口分析（工程师补充「已有设备绑定、刀库更新接口」）

- App 已对接 REST：`/api/v1/materials`、`/models/{id}`、`/tasks`、`/devices/{id}/jobs`、`/diagnostics`。
- **设备绑定**：阿里云接口是绑定唯一权威源（写 DB）；固件 `hello` 仅连接层身份声明；腾讯云 broker ACL 由阿里云绑定事件跨云驱动。三者互补无冲突。
- **刀库更新**：必须区分「刀库目录(catalog，阿里云 DB 资产，REST 持久化)」与「刀仓映射(tool map，固件运行时，MQTT `toolMap` 应用)」。建议 REST 只管目录持久化，运行时应用只走 MQTT `toolMap`，阿里云 API 不直接发 `toolMap` 给固件，避免三写冲突。
- 待拍板项已于 **2026-08-14 由 App 任务拍板**，详见 **§18**。

---

## 18. 四项跨云决策（2026-08-14 由 App 任务拍板，四方执行）

### Q1 阿里云 ↔ Broker 连接方式：常驻 bridge 持久客户端（单一通道）
- 阿里云 API 服务器以持久 MQTT 客户端身份 **`bridge-aliyun`**（TLS 8883，私有证书）常连腾讯云 broker。
- 该客户端**统一承载三类跨云下发**：① 设备绑定/解绑 → ACL 同步控制消息；② job prepare 等命令 → `cnc/<deviceId>/cmd`；③ 刀库目录更新 → `cnc/<deviceId>/tool_catalog`（retain）。
- **不引入 broker REST admin 面**：ACL 同步通过 `bridge-aliyun` 发布控制消息到特权主题 `cnc/sys/acl`，由 broker 认证后端消费并应用，避免双通道维护。
- 收益：pub/sub 语义统一、QoS/retain 可控、阿里云侧只维护一个长连接。

### Q2 刀库更新是否推 toolMap：否
- 刀库目录（catalog）更新 → **只广播** `cnc/<deviceId>/tool_catalog`（retain=true）。
- **绝不推送 `toolMap`**。`toolMap` 严格是机器运行时命令，由操作员/App 在 W4 步骤下发，机器拥有。
- 理由：目录是云资产、ToolMap 是机器运行时真值，语义不同；禁止阿里云推 toolMap 可杜绝「REST 改目录 + API 推 map + App 推 map」三写冲突。

### Q3 G-code 文件服务位置：统一阿里云
- G-code 文件**存储 / 服务 / 「push to MCU」编排**全部落在阿里云 037123.xyz（与 API/DB 同云）。
- 字节传输：机器经 HTTPS 从阿里云下载（device token 鉴权）；broker 仅承载 job 命令 + G-code URL（**不传字节**），与「App 绝不持有 G-code」资产闭环一致。
- 腾讯云保持无状态：仅 broker + 视频流。

### Q4 ACL 同步失败重试策略（至少一次 + 幂等 + 持久补偿 + 重连重放 + 周期对账）
- **幂等键**：每个绑定/解绑事件生成 `aclOpId`（复用绑定记录 id），重试安全、不重复授权。
- **持久队列**：阿里云 DB 存 `acl_sync` 表，状态 `pending→acked→failed`，含 opId / 目标 machineCode / accountId / payload / retryCount / nextRetryAt。
- **指数退避**：1s→5s→30s→2min→10min→1h，最多 **8 次**；耗尽标 `failed` 并告警（向运维/账号推 notify）。
- **三重重投**：① 失败立即排程；② `bridge-aliyun` 重连成功时重放所有 `pending`；③ 每 **5min** 全量对账（DB 绑定 vs broker ACL 实际态），修复漂移。
- **成功判据**：broker 认证后端处理控制消息后回 ACK（`bridge-aliyun` 收 `cnc/sys/acl/ack` 或 DB 回写）；**5s 超时**无 ACK 视为失败重试。
- **可见性**：绑定页展示同步态（同步中 / 已生效 / 失败），失败不让用户误判绑定成功。
- **降级**：broker 长期不可达 → 绑定仍写 DB 成功（业务可用），ACL 未生效；broker 恢复后由重连重放自动补；页面提示「网络恢复后将自动授权」。
