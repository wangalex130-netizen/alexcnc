# 系统级架构复核报告（2026-08-30 · 总架构师视角）

> **本报告的视角**：不是检查某个接口对不对，而是**按客户的完整使用旅程走一遍**，
> 并按**批量生产**的要求检查每个环节能否自动化。
> 前序文档（36/37/38）偏"各端待办"，本篇偏"这套系统能不能真跑起来"。
>
> 校核方法：读代码 + 读配置 + 对照实测截图与 MQTT 部署报告，凡无出处的一律标注「未确认」。

---

## 零、先走一遍客户的完整旅程

```
① 工厂生产机器（屏幕/摄像头各自烧码）
      ↓
② 客户开箱通电，设备联网
      ↓
③ 客户下载 App、注册账号
      ↓
④ 客户绑定自己的机器
      ↓
⑤ 客户选机器、看摄像头画面
      ↓
⑥ 客户选模型、开始雕刻
      ↓
⑦ 加工中（锁屏？切微信？网络抖动？）
      ↓
⑧ 加工完成
```

**结论先行**：第 ③④⑤⑥ 步在**单台手工调试**下能跑通，
但 **③ 和 ④ 在量产下走不通**（需要人工介入），
**⑤ 存在隐私事故**，**⑦ 可能导致工件报废**。

---

## 一、P0 · 阻塞量产 / 安全事故（必须解决才能卖）

### P0-1 🔴 摄像头画面可被任意人观看（隐私事故）

**事实链**（全部读代码/配置得出）：

| 环节 | 现状 | 出处 |
|---|---|---|
| 中继拉流 token | **硬编码在 App 里**：`lunyee-cnc-relay-7k2p` | `app/config.dart:34-37` |
| token 是否按设备/账号区分 | **否**，所有设备共用同一个 | 同上，全局单一常量 |
| 中继是否校验"该账号有权看这台设备" | **否**，`REQUIRE_BINDING` 未开启 | `machines_service.dart:53-54` 注释：「缺省为空 = 兼容期 demo 放行」 |

**后果**：任何人只要知道这个 token（**逆向 APK 就能拿到明文**，或从摄像头端问一句就有），
即可访问：

```
http://39.106.144.53:8080/stream/<任意设备码>?token=lunyee-cnc-relay-7k2p
```

查看**任意一台**客户机器上的摄像头画面。客户装在工作室/家里的摄像头被陌生人实时观看。

**这不需要黑客技术**，只需要把 APK 解包。

**处置（两端必须一起做）**：
- 阿里云：**P-2 必须提前到 P0** —— 登录后下发**按账号签发**的 token（JWT，含 userId + 允许的设备列表 + 有效期）
- 中继：开启 `REQUIRE_BINDING`，校验 `user=` 参数与 token 中的设备权限
- App：不再使用硬编码 token，改用登录后下发的（**我这边改，但要等后端先给接口**）

> 现状是"demo 兼容期"设计，**在卖第一台机器之前必须换掉**。

### P0-2 🔴 客户无法自助注册账号

**事实**：`login_page.dart:6/102` 明确注释「2026-08-21 工程师确认：App 不开放注册功能，登录页隐藏注册入口」。
`auth_service.dart:16` 也写「`POST /api/auth/register` 为方案 A 假设，**后端当前未开放注册**」。

**后果**：B2C 客户买了机器、装了 App，**没法自己创建账号**。
只能靠人工在后台开户再把账号密码发给客户 —— 这在批量销售下完全不可行。

**这是量产的硬阻塞**，优先级高于目前所有的 MQTT/摄像头细节。

### P0-3 🔴 机器档案必须人工在后台创建

**事实**：`machines_service.dart:120-121` 注释「线上后端要求先有机器档案（machineId），扫码只能匹配账号中**已创建**的机器」。
且实测账号列表里有一台「**未配置机器码**」——说明人工建档流程会漏字段，且**没有校验拦截**。

**后果**：客户拿到机器扫码绑定 → 后端没有这台机器的档案 → 绑定失败。
流程断裂，且客户不知道发生了什么。

**需要阿里云回答**（这决定了整个开机流程怎么设计）：
1. 机器档案是**工厂出货时批量导入**，还是客户扫码时**自动创建**？
2. 机器码（如 `cnc-demo-03`）由谁生成、写在哪台设备上？
3. 扫码时若档案不存在，能否自动建档并与当前账号绑定？

### P0-4 🔴 摄像头设备码硬编码在固件里，无法批量生产

**事实**：摄像头端回报「设备码固件里**三处强制赋值**」（`grep` 二进制确认 03×1 / 01×0）。

**后果**：每生产一台机器，都要**单独编译一次摄像头固件**并烧录。
这在产线上不可接受，也做不到"屏幕码 = 摄像头码"的一致性保证。

**量产方案**（需与摄像头端对齐）：
- 设备码改为**产线烧录进 NVS**（一次写入，固件通用）；或
- 设备自注册（provisioning）：首次上电用屏幕的唯一码去后端换 token，摄像头从屏幕获取

> 这正好是「5 道门」里的第 5 道 —— demo 期手工做可以，量产必须自动。

---

## 二、P1 · 可靠性 / 加工安全（会导致工件报废）

### P1-1 🔴 加工中网络抖动或锁屏 → 心跳中断 → 工件报废

**事实链**：

| 环节 | 数值 / 行为 | 出处 |
|---|---|---|
| 固件 Feed Hold 阈值 | **15 秒**收不到命令即进入 Feed Hold | `hardware_service_real.dart:50,671` 注释 |
| App 心跳周期 | **10 秒**发一次 `{"cmd":"hello"}` | `hardware_service_real.dart:51` |
| **安全余量** | **仅 5 秒** | 15 − 10 |
| MQTT 断线时的行为 | **`_publish` 静默丢弃，不报错、不通知** | `hardware_service_real.dart:636` |
| App 切后台/锁屏处理 | **完全没有**（全库仅 console_page 在 `resumed` 时探测网络，**无 `paused` 处理**） | `grep didChangeAppLifecycleState` 仅 1 处 |

**场景复现**：

> 客户启动一个 40 分钟的雕刻 → 中途锁屏去看别的东西 / 切到微信回消息 /
> Wi-Fi 抖了一下 → MQTT 断连或心跳发不出去 → **15 秒后固件 Feed Hold** →
> 主轴继续转但进给停止 → **工件烧糊 / 断刀 / 报废**。
> 而 App 端**没有任何提示**（`_publish` 直接 return）。

**这是客户视角下最严重的问题**：不是"用不了"，是"用着用着把料废了"。

**需要各端一起决定**（我不能单方面改）：

| 方案 | 归属 | 说明 |
|---|---|---|
| A. 固件放宽阈值（15s → 60s+） | 固件 | 最简单、最有效。**但需确认放宽后安全门是否还成立** |
| B. App 加工中禁止锁屏 + 前台保活 | App | 用 wakelock + Android 前台服务 |
| C. 心跳断连时强提示 + 自动暂停 | App | 至少让客户知道出事了 |
| D. 心跳周期缩短（10s → 5s） | App | 增大余量，但增加流量 |

> **建议 A + C 组合**：固件放宽阈值给容错，App 在断连时明确告警。
> **这条必须与固件端对齐，不能 App 单方面决定。**

### P1-2 🟠 退后台未停止推流 → 摄像头持续推流

**事实**：全屏预览页只在 `dispose()` 时发 `stream_stop`。
客户按 Home 键退出时，**页面没有 dispose** → 我加的 30s 续租会一直跑 → 摄像头一直推。

**后果**：
- 违背"按需推流"的设计初衷（省带宽/省电/护隐私）
- 客户以为关了，其实摄像头还在拍

**修法**（App 侧，我可做）：监听 `AppLifecycleState.paused` → 发 `stream_stop`；`resumed` → 发 `stream_start`。
**但需注意**：加工中退后台不能停推（客户可能想边干别的边看监控）——需要区分"在看监控"和"不在看"。

> 建议口径：**退后台即停推**；客户回到前台自动恢复。加工中若需持续监控，走延时摄影而非实时流。

---

## 三、P2 · 体验与一致性

| # | 问题 | 影响 | 归属 |
|---|---|---|---|
| P2-1 | 必须手动点选机器（`isDefault` 解析了但没用） | 只有一台机器时仍要手动点，多余 | App（A-5） |
| P2-2 | 中继单点在北京 `39.106.144.53:8080` | **与"旗舰款主攻海外 C 端"的战略矛盾** —— 海外客户跨国拉流延迟高 | 需产品/架构决策：是否上 CDN / 就近接入 / 多区域中继 |
| P2-3 | 摄像头如何配网（未确认） | 配网职责归屏幕端，但摄像头也要连同一个 Wi-Fi 才能推流。**客户要配两次网？** | 摄像头 / 固件 |
| P2-4 | 空拉流地址报「无信号」 | 措辞不准，客户会以为摄像头坏了 | App（N-4） |
| P2-5 | 一账号多机的切换体验 | 实测账号下有 4 台，切换后全局重建连接（会短暂断连） | 观察项 |

---

## 四、代码层校核结果（本次改动）

| 项 | 结论 |
|---|---|
| A-1 空 deviceId 不建连 | ✅ 无 bug。`LinkState` 初始 `disconnected`，不会出现"假已连接" |
| A-1 空拉流地址 | ✅ 不崩溃。播放器 `Uri.parse` 在 try 内，走 `_onError` |
| A-2 `onSubscribeFail` | ✅ 类型正确（`SubscribeCallback = void Function(String)`），v3.1.1 可用 |
| A-3 cam 订阅与分发 | ✅ `_handleCam` 在 `_parseAndEmit` **之前** return，摄像头帧不会污染机器状态（不会误解锁 Jog） |
| A-6 续租与 stop 顺序 | ✅ `dispose` 先 cancel timer 再发 `stream_stop`，不会被续租"复活" |
| 心跳 QoS | ✅ `atLeastOnce`（QoS1），会重传 |
| 全屏页传参 | ✅ console 入口传 `currentMachineProvider`，与实例 deviceId 同源；⚠️ 机器列表入口不同源（N-3，不影响画面） |

---

## 五、需要各端确认 / 对齐的清单（按优先级）

### 🔴 阿里云（PC 工程师）—— 决定量产能否成立

1. **注册接口何时开放？**（P0-2）客户自助注册的排期。
2. **机器档案如何产生？**（P0-3）工厂批量导入 / 客户扫码自动建档 / 其他？
3. **按账号签发的拉流 token 接口**（P0-1，从 P-2 提前到 P0）：字段、有效期、如何下发。
4. 中继 `REQUIRE_BINDING` 谁负责开启？鉴权逻辑按 userId 还是按设备列表？
5. （原有）P-3 刀仓 `bit-config` slot 语义。

### 🔴 固件（屏幕 / 主控）

6. **Feed Hold 15 秒能否放宽到 60 秒？**（P1-1）这是加工安全的首要项。
7. 放宽后，App 断连时固件应如何处理（暂停？报警？继续？）
8. 屏幕与摄像头是否共用同一设备码？产线如何写入？（P0-4）

### 🟠 摄像头

9. **`stream_start` 是否幂等？**（C-6，阻塞 A-6 的安全使用）
10. **摄像头如何配网？**（P2-3）是否与屏幕共用一次配网？
11. 设备码能否改为 NVS 烧录 / 自注册？（P0-4）
12. （原有）C-1 心跳容忍、C-2 看门狗（须与 App 续租配套）。

### 🟡 MQTT / broker

13. M-5：`acl.conf` 给 `app-demo` 订阅加 `cnc/+/cam`
14. M-1 / M-3（两文件）/ M-4：见 docs/38

### 🟡 产品 / 架构决策（需要你拍板）

15. **海外战略 vs 北京单中继**：是否规划多区域中继 / CDN？（P2-2）
16. **加工中退后台策略**：停推（省资源）还是保持（可监控）？（P1-2）
17. **安防底线**：在 P0-1 解决前，是否暂停对外销售 / 只发内部测试机？

---

## 六、一句话总结

> **单台联调的技术链路已经打通**（ACL 通配、摄像头切 03、端到端 159KB 真实帧都验证了），
> 但**这套系统目前还停留在"能演示"，没有到"能交付"**：
> 客户不能自助注册、机器档案要人工建、摄像头码要人工编译固件、
> 摄像头画面任何人都能看、加工中网络一抖工件就废。
>
> **前三件决定能不能卖，第四件决定敢不敢卖，第五件决定客户用得爽不爽。**
