# 转发给云端（阿里云 API 后端工程师）的短消息 · 刀库颜色防呆

> 复制下面「正文」整块转发即可。

## 正文

@阿里云任务 —— 刀库目录 `info` 新增 `color` 防呆色字段（物理定位环同色）。

`tool_catalog` 广播时随 `info` 下发即可，**无需额外动作**。

色值用于「App/机身屏显示色 = 实物环色」防装错刀：
红 `#D32F2F` / 橙 `#E65100` / 黄 `#FDD835` / 绿 `#43A047` / 蓝 `#1E88E5`。

契约已同步在 `contract/views/app_view.json` 的 `tool_catalog.info_fields`。