# 对《阶段基点复核回复与待答问题》的回复（App 侧）

> 回复对象：《阶段基点复核回复与待答问题-20260911》
> 方法：**逐条独立复核源码**（不采信任何一方自述），每条给 `文件:行号`
> 结论：**4 处修正我接受 3 处、部分接受 1 处**；11 个问题逐条作答；另**提供 3 项对方尚未掌握的新事实**。

---

## 一、对"执行前必须修正的 4 处"的核对

### 2.1 P0 止血会先把 App 锁死 —— ✅ **成立，我接受，方案改影子模式**

我独立复核，三条证据全部成立，并**补一条根因**：

| 证据 | 我的复核 |
|---|---|
| `_sendTcp` 只有定义、无调用 | ✅ 全 `lib/` 检索仅 `hardware_service_real.dart:997` 一处（定义），**零调用** |
| `_publish` 未连接即失败 | ✅ `:765-767` 直接 `return false` |
| `connect()` 云端模式不连 TCP | ✅ `:262-266` `if (cloudEnabled) { await _connectMqtt(); return; }` |
| **根因（我补充）** | ✅ `providers.dart:109` **硬编码 `cloudEnabled: true`**（`hardware_service_real.dart:141` 默认为 false，被 provider 覆盖）⇒ 正式包与联调包**都**走 MQTT |

**结论**：固件一旦开 `is_wan=true` 真拒绝，App 的点动/回零/主轴/解锁/设零点**全部 E409**。→ **P0 只能用影子模式**，我接受。
**同时接受你们的第二条**：白名单检查必须**前移到 `gcode_ready` 分发之前**（`cnc_net.c:1393-1398` 现在在 `:1416` 之前 return，这是真实绕过）。

### 2.2 机身确认可只改固件、App 零改动 —— ✅ **成立，且我提供更强的证据**

你们说"App 不改"基于 90 秒看门狗与横幅；我复核后**证据比你们更硬**：

| 项 | 位置 | 复核结果 |
|---|---|---|
| 看门狗 90s | `hardware_service_real.dart:1171` | ✅ 确认；且触发条件仅 `preparing`/`confirming`，**60s 回执不会触发** ✅ |
| 横幅文案**已存在** | `job_launch_banner.dart:92-94` | ✅ 原文即"**请在机器上按开始键确认，确认后才会动刀。**" |
| 机旁确认订阅 | `console_page.dart:2009-2011` | ✅ 已按 `status.awaitingConfirm` + notify `confirm_required` 触发 |
| 自检页等待确认 | `self_check_page.dart:152-154` | ✅ 已实现"等待客户在机器上确认" |

**⇒ App 零改动成立，我同意把 P0-1 提到 P0。**
**一个补充（我这边自办，不阻塞）**：`ECONFIRM_TIMEOUT` 目前**无文案映射**（`_ackCodeText` 只覆盖 `E400/E403/E409/E500/E501/ETIMEOUT/ENETDOWN/EVERIFY`），会落到兜底文案"机器没能完成这条指令，请重试"。我补一条映射即可（L0，半天）。
**另需固件配合**：延迟回执期间请**保持 `awaitingConfirm=true` 并可发 `confirm_required`**，否则 App 横幅不会出现。

### 2.3 C-01 消失 + 单方换证书会让屏幕集体掉线 —— ✅ **成立，我接受并行监听方案**

我复核固件的证书路径，与你们一致：
- 固件 MQTT 用**内置私有 CA**（`cnc_net.c:2236-2239`：从 `ca_crt_start` 取、并 `skip_cert_common_name_check = true`）；
- 公共证书集只用于 **G-code HTTPS 下载**（`:898`）。

⇒ 后端单方面换 8883 证书，**所有屏幕立即掉线**。同意"新开域名+公共证书监听、旧监听并行保留、逐端切换"。
同意"**先换 App 账号，屏幕账号待确认口令是否为公开泄露那组后再动**"。

### 2.4 冻结格式 vs 冻结语义 —— ⚠️ **同意，但请加一条**

同意 v1 冻结**帧格式**（`run/hold`、进度 0–100、`jobState`），语义缺陷单列。**但"已知缺陷清单"必须带修复计划与版本**，否则会变成"永久豁免"。建议每条四列：

`缺陷 | 影响 | 计划修复版本 | 责任端`

例：`awaitingConfirm 语义=文件已下载 | 安全语义错位 | v1.1（随 P0-1 一并修） | 固件`。

---

## 二、逐条回答（A-Q1 ～ A-Q11）

| # | 回答 | 依据 |
|---|---|---|
| **A-Q1** | **是，确认成立**。`cloudEnabled` 在 `providers.dart:109` **硬编码 true**（类默认 `false` 被覆盖）⇒ 正式包/联调包都是 true；**没有任何构建会经 TCP 8899 发命令**（`_sendTcp` 零调用） | `hardware_service_real.dart:141/262-266/765-767/997`、`providers.dart:109` |
| **A-Q2** | **工作量 1–2 天（不含实机联调）**。代码已保留：`_sendTcp`（`:997`）+ `connect()` 局域网分支（`:268+` beacon 发现）+ `_tcp/_tcpConnected` 状态。需做：① `_dispatch` 加回"同网→TCP"分支；② **保持 MQTT 连接**——因为**回执只从 MQTT notify 回来**（固件 LAN 路径不单独发 ack，`handle_command` 统一解析后 `cnc_net_publish_cmd_ack` 仍发 notify）；③ 复用 `_confirmLan()` 判定。**风险**：8899 无鉴权 ⇒ **必须先做配对令牌**，同意你们的顺序。**关键提示**：TCP 只是"命令通道加速"，**不是 MQTT 的替代**（否则丢回执） | `hardware_service_real.dart:997/268+`、`cnc_net.c:1416-1419` |
| **A-Q3** | **是，与你们推断一致**：confirming 阶段 60s 延迟回执**不触发** 90s 看门狗；横幅显示"请在机器上按开始键确认"。若回 `ok=false + ECONFIRM_TIMEOUT` → App 走 `_friendlyAckText`→`_ackCodeText`，**当前无该码映射** → 显示兜底文案。**我接受 `ECONFIRM_TIMEOUT` 这个码名**，App 侧补映射（L0）。**前置**：固件须保持 `awaitingConfirm=true` 并发 `confirm_required` | `:1171`、`job_launch_banner.dart:92-94`、`:1420-1440`（`_ackCodeText` 映射表） |
| **A-Q4** | **可以**。我把《三端对齐校核报告-20260911》推到 `alexcnc` 仓库 `docs/`（我有该仓写权限），推完给出路径。若要对齐仓也留一份，需给我对齐仓写入权限，或我直接提供文件由你们转存 | — |
| **A-Q5** | **App：当前 main = `c803772b`**（其中 `0d7eee8d` 是 09-10 的 P0/P1 修复主体；`c803772b` 仅追加 docs）。**固件与后端我无法提供**：我手上只有解压副本、**无 git 元数据**，请两端自报 commit | CI run #341/#342 |
| **A-Q6** | **状态映射：只做了 `run`/`running` → `busy`**（`:163-168`）；**`hold`/`home`/`jog` 全部未映射** → 落 `unknown`（安全侧=Jog 保持锁定，但暂停/回零/点动中会显示"状态未知"）。**进度**：已兼容 0–100（`>1` 则 `/100`，`:200-204`）✅。**其他未适配**：`jobState`（App 未解析）、`uptimeMs`（待固件改名）、`scTotal`（App 用 `index>=total` 判完成，而固件常驻 5）。**工作量：0.5–1 天**（按 v1 的 `machineState` 词表重写映射 + 单测） | `machine_status.dart:163-168/200-204`、`cnc_net.c:493-494` |
| **A-Q7** | **同意去掉周期 hello**。现状：App 每 10s 发一次（`_heartbeatInterval = 10s`，`:65`），固件收到即忽略 ⇒ 纯开销。**建议**：降为"**连接建立后发一次**"（保留身份声明语义），或完全停发（固件已有 LWT + retained status 做在线判定）。我倾向"**连接后一次 + 断线重连时一次**" | `hardware_service_real.dart:65`、固件 `:571-575`（忽略 hello） |
| **A-Q8** | **能，但目前只停本地定时器、不发任何消息**：`_stop()` 已挂在 `onTapUp`/`onTapCancel`/`onLongPressEnd`（`:334-336`），`dispose()` 也取消（`:323-325`）——**但这只阻止后续帧，已发出的帧仍会执行**（这正是"松手继续走"）。要发 `jog_cancel` **可以**（fire-and-forget，不需要 ack）。**缺口：App 进后台没有任何处理**（无 `WidgetsBindingObserver`）→ 需补 `didChangeAppLifecycleState(paused)` 时发 `jog_cancel`。**按协议版本切新旧模式：可以**（固件在 status/hello 上报 `proto`/`schemaVersion` 即可，App 侧按字段分流） | `jog_sheet.dart:316-336` |
| **A-Q9** | **`v11MqttTopicsEnabled` 默认 true**（`config.dart:343-347`）⇒ **确实在订阅**。**固件不发布** `cnc/<id>/job` 与 `cnc/<id>/sys`（我 grep 固件全部 `cnc_net_publish_*`，无这两个主题）⇒ **两条空订阅**，同意清理（A-2 一并做）。**补充**：驱动新版也订阅了 `cnc/<id>/log`（`MqttDeviceGateway.java:71`），而固件同样不发布 `log` → 也是空订阅 | `config.dart:343-347`、固件 `cnc_net.c`（无生产者）、`MqttDeviceGateway.java:67-71` |
| **A-Q10** | **同意**整套协作机制（对齐仓四目录、ADR 编号、"作废即清洗"、docs 去重为链接）。App 侧我会把 `alexcnc/docs` 中与平台重复的文档改为指向对齐仓的链接 | — |
| **A-Q11** | **同意 P0 改为你们 §5.1 版本**（P0-1 提前、P0-2 影子模式、P0-3 并行监听）。**两条补充**：① P0-1 的前置是"3020 是否真有 GPIO4/5 按键"，请先确认（O-Q1），若无则用屏幕按钮替代；② P0-5 文档清洗中，**App 侧注释由我清**（`hardware_service_real.dart:1453-1463`），并把**我那份校核报告也纳入清洗范围**（它含已作废的 F-01 建议） | — |

---

## 三、我提供的新事实（对方尚未掌握，影响 B2 / O-Q6 的结论）

### 3.1 驱动仓库**已有 MQTT 网关** —— B2 末句与 O-Q6 需更正

你们基于 `artimaker-driver-develop` 得出"驱动仓库中没有任何 MQTT 代码"。**娄鑫 09-11 提供的是新仓库 `artimaker-driver-gcode-migration`，里面已有完整 MQTT 实现**：

| 文件 | 行数 | 作用 |
|---|---|---|
| `driver/.../websocket/mqtt/MqttDeviceGateway.java` | **894** | 实现 `MqttCallbackExtended`；订阅 `cnc/<id>/status`、`/notify`、`/telemetry`、`/log`、`cnc/broadcast/#`；向 `cnc/<id>/cmd` 发布 QoS1 + `PendingCommand` 登记 |
| `DeviceState.java` / `MqttSettings.java` / `PendingCommand.java` | 183 / 211 / 69 | 设备状态、配置、待确认命令 |
| 另有 3 个单测 | 102 / 228 / 56 | 含 `MqttDeviceGatewayTest` |

⇒ **O-Q6（"前端依赖的驱动→MQTT→小屏通道源码在哪里"）已可回答：在新仓库里，且已经存在**。
⇒ 建议：**B2 的结论改为"驱动旧版无 MQTT、新版正在迁移并已具备该通道"**，并把驱动新版纳入看板。

### 3.2 前端依赖的通道确实存在（已从 zip 抽取核对）

- `src/store/modules/webSocket.js:53` 注释即 "**Driver -> MQTT -> 小屏模式的设备与作业状态**" —— 前端依赖的正是 3.1 中那个通道，**它已经存在**。
- 因此 B2 里"前端依赖的能力不在驱动仓库里"这一条，**在新版驱动下不成立**（旧版成立）。

### 3.3 端口扫描口径（供三方对齐时留意）

驱动扫描 `{80, 8080}`（`DeviceControlService.java:2509`、`CncScannerService`），而固件侧：**命令通道 = 8899**、**HTTP 文件服务 = 80**。
⇒ "驱动找设备"与"固件对外开放的口"口径不同，属于 B3 之外的另一处不一致，建议并入契约 v1 的端口表。

---

## 四、我方（App）本轮的行动承诺

| # | 事项 | 层级 | 何时 |
|---|---|---|---|
| 1 | 校核报告推入 `alexcnc/docs` 并给路径 | L0 | 今天 |
| 2 | 清理"按 ts 丢弃 >500ms"过时注释（`:1453-1463`） | L0 | 今天 |
| 3 | 删除 `SysInfo.uptime` 减法（无数据来源） | L0 | 今天 |
| 4 | 补 `ECONFIRM_TIMEOUT` 文案映射 | L0 | 半天 |
| 5 | 连接失败原因分类提示（TLS/凭据/网络） | L0 | 1 天 |
| 6 | `state` 映射按契约 v1 重写（`hold`/`home`/`jog`）+ `jobState` 解析 | L1（等 v1 冻结） | v1 冻结后 1 天 |
| 7 | `hello` 降频 / `jog_cancel`（含后台）+ 本地 TCP 通道恢复 | L1（依赖固件） | 待双方排期 |

---

## 五、两处仍需道长/其他端回答才能推进（我方无法自行确认）

1. **O-Q1 硬件**：3020 量产机是否有 GPIO4/5（START/STOP）物理按键 —— 决定 P0-1 形态。
2. **O-Q2 固件/控制板**：改版 GRBL 是否支持 `0x85` 点动取消 —— S1 的前提，必须实机验证。
