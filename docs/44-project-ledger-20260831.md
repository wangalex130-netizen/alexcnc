# 项目总账本 —— 副总视角（2026-08-31）

> **用途**：汇总当前所有端口的问题、阻塞、进度，方便快速掌握项目状态。
> **当前聚焦**：`cnc-demo-03` 真机联调贯通（01/02 在硬件工程师那里，暂不投入）。
> **更新规则**：每改完一项由各端口负责人回填状态；跨端会议前以此为 baseline。

---

## 一、项目总览

| 项 | 状态 |
|---|---|
| 目标 | 让 `Lunyee@517788.xyz` 账号下的 `cnc-demo-03` 真机：App 能稳定连接、能看到摄像头画面、能发控制命令 |
| 当前阶段 | 真机联调期（不再是纯 demo / 延时模式） |
| 唯一真机 | `cnc-demo-03`（屏幕 + 摄像头） |
| 测试账号 | `Lunyee@517788.xyz / q12345678` |
| 关键服务器 | MQTT broker `43.154.192.242:8883`；中继 `39.106.144.53:8080`；后端 `https://037123.xyz` |

---

## 二、各端口状态总表

| 端口 | 负责人 | 当前状态 | 关键阻塞 | 下一步 |
|---|---|---|---|---|
| **App（Flutter）** | App 团队 / 本助手 | ⚠️ 部分可用 | 1. 选 03 后 MQTT 反复闪断<br>2. 摄像头预览打不开（已由用户定位：WiFi 切回 Lunyee-YY，等摄像头端改）<br>3. 机器列表 03 显示不在线 | 1. 排查 MQTT 闪断根因<br>2. 摄像头恢复后复测预览<br>3. 确认后端 `online` 字段来源 |
| **摄像头固件 / 中继** | 隔壁 AI（拟移交 PC 工程师） | ⚠️ 在线但 WiFi 切错 | 当前连到 Lunyee-YY，需切到目标 WiFi | 切回目标网络后确认 RTSP/MQTT 恢复 |
| **MQTT 服务器** | MQTT 任务 / 运维 | ⚠️ 基础可用 | 1. `acl.conf` 缺 `cnc/+/cam` 给 app-demo 订阅（M-5）<br>2. 仓库 compose 与线上运行文件不一致（M-6）<br>3. `DENY_ACTION` 仓库未对齐线上（M-3） | 按 `docs/41` 执行 M-1/M-3/M-5/M-6 |
| **屏幕 / 主控固件** | 固件工程师 | ⚠️ 待联调 | 已确认代码版本已转交，旧代码审计作废；需按 `docs/43` 契约实现 | 回答 G1-G6，落实运动安全与状态帧契约 |
| **PC 后端 / 阿里云** | PC 工程师 | ⏳ 待开发 | 1. P0-1 中继 token 按账号签发（`docs/40`）<br>2. 云网关从 `admin` 切到 `svc-bridge-aliyun-api`<br>3. 设备在线状态字段来源待确认 | 确认 P0-1 接口字段；提供在线状态判定逻辑 |

---

## 三、当前阻塞清单（按优先级）

| # | 问题 | 影响 | 负责端 | 状态 |
|---|---|---|---|---|
| P1 | **App 选 03 后 MQTT 反复闪断** | 命令/状态无法同步，所有控制失效 | App + MQTT | 🔴 排查中 |
| P2 | **摄像头当前在 Lunyee-YY WiFi，不在目标网络** | App 预览无画面 | 摄像头 | 🟡 用户已通知修改 |
| P3 | **机器列表 03 显示"不在线"** | 客户无法判断哪台机器可用 | PC 后端 | 🟡 需确认在线判定逻辑 |
| P4 | **MQTT ACL 缺 `cnc/+/cam` 订阅权限** | App 收不到摄像头 streaming 回执 | MQTT | 🟡 待执行 M-5 |
| P5 | **Feed Hold 依赖 App 心跳（P0-5）** | 客户锁屏/切网络 15s 后雕刻中断 | 固件 | 🟡 待固件回答 G1 |
| P6 | **设备码格式未最终定死** | 影响屏幕、摄像头、阿里云三端一致性 | 产品/固件 | 🟡 待拍板 |
| P7 | **摄像头工作移交 PC 工程师需收口** | 避免职责真空、接口混乱 | 产品/PC 工程师 | 🟡 待启动 |

---

## 四、03 真机联调检查项

### 4.1 必通链路（当前阻塞 = 无法卖机器）

- [ ] 03 屏幕固件 MQTT 上线（broker 能看到 `screen-cnc-demo-03`）
- [ ] 03 摄像头 MQTT 上线（broker 能看到 `cam-cnc-demo-03`）
- [ ] App 选 03 后 MQTT 稳定不闪断
- [ ] App 能订阅到 `cnc/cnc-demo-03/status` 并显示真实坐标/状态
- [ ] App 能发 Jog / Home / 辅助开关命令，机器有动作
- [ ] App 打开预览 → 摄像头开始推流 → 画面出来
- [ ] App 退出预览 → 摄像头停止推流
- [ ] 后端 `/api/machine/list` 返回 03 `online: true`

### 4.2 安全红线（联调期就要守住）

- [ ] App 断链/锁屏不影响机器运动安全
- [ ] 加工中 Jog 被正确锁定
- [ ] 急停/限位/仓盖本地闭环

---

## 五、时间线（最近 7 天）

| 时间 | 事件 |
|---|---|
| 08-29 | 摄像头 ACL 由 `cnc/+/status` 改 `cnc/+/cam`；App 加 `_isCameraStatusFrame()` 兜底 |
| 08-30 13:35 | MQTT 部署完成：`app-demo` 改通配、`cam-cnc-demo-03` / `svc-bridge-aliyun-api` 建号、`cnc-relay` 删除；两条验收通过 |
| 08-30 17:05 | 真机截图确认 `Lunyee@517788.xyz` 下有四台机器（03/02/未配置/01） |
| 08-30 深夜 | 摄像头端确认 `stream_start` 不幂等，承诺修复 C-6；确认方案 C（通用固件 + 配网下发设备码） |
| 08-31 上午 | owner 确认 P0-1 中继鉴权必做；生成 `docs/40` 给 PC 工程师 |
| 08-31 11:43 | 重新核对 MQTT 待办，生成 `docs/41`；发现 M-6（服务器 compose 文件不在仓库） |
| 08-31 12:00 | 旧 `cnc-fw` 代码审计生成 `docs/42`；随后 user 指出该代码已转交工程师、版本落后 |
| 08-31 13:00 | 改出跨版本契约清单 `docs/43`；发现 `spindle` 字段冲突、`state` 越界回落 idle 等字段级陷阱 |
| 08-31 13:46 | 用户实测：App 选 03 MQTT 反复闪断，摄像头无画面；后定位摄像头 WiFi 切回 Lunyee-YY |

---

## 六、下一步动作与负责人

| 动作 | 负责端 | 交付物 |
|---|---|---|
| ① 摄像头切回目标 WiFi 并确认 RTSP + MQTT 恢复 | 摄像头任务 | 在线截图 / 推流测试 |
| ② 排查 App MQTT 闪断根因 | App 任务 + MQTT 任务 | 日志 / broker 连接记录 / 修复方案 |
| ③ 确认后端 `online` 字段判定逻辑 | PC 后端 | 字段来源说明 / 是否为实时 |
| ④ MQTT 侧执行 M-5（补 `cnc/+/cam` 订阅） | MQTT 任务 | 部署后验收 |
| ⑤ 固件侧回答 docs/43 的 G1-G6 | 固件任务 | 书面答复 / 改造计划 |
| ⑥ 启动摄像头工作向 PC 工程师移交的收口 | 产品 + PC 工程师 | 移交清单 / 接口负责人 |

---

## 七、副总需要拍板的事项

| # | 问题 | 建议 |
|---|---|---|
| 1 | **设备码格式定死了吗？** | 建议小写 + 数字/连字符，如 `cnc-demo-03`，满足 MQTT ACL 正则 `^[a-z0-9-]+$` |
| 2 | **App 能不能远程发起一次雕刻？** | 建议 V1 只做「机身上启动」，App 只做准备/参数下发；后期再加远程启动 |
| 3 | **摄像头工作移交 PC 工程师，范围包括哪些？** | 建议：摄像头固件 + 推流/中继服务器 + 与 MQTT 的联动 + App 摄像头模块的接口定义；**不包括** App UI 实现 |
| 4 | **下载进度要不要在 App 展示？** | 建议展示（客户需要看到"正在准备文件"） |

---

## 八、可直接转发给各端

> **给摄像头任务**：
> 03 当前连到 Lunyee-YY WiFi，请切回目标网络后确认：
> 1. `cam-cnc-demo-03` 在 MQTT broker 在线；
> 2. RTSP 流可用；
> 3. 收到 `{"action":"stream_start"}` 后开始推流，收到 `stream_stop` 后停止。
>
> **给 MQTT 任务**：
> App 选 03 后 MQTT 反复闪断，请在 Dashboard / 日志里查 `android-cnc-demo-03` 的连接记录，
> 看是网络原因、clientId 互踢、还是 ACL 拒绝。同时尽快执行 M-5（`acl.conf` 加 `cnc/+/cam` 订阅）。
>
> **给 App 任务**：
> 摄像头无画面先等摄像头端切 WiFi；MQTT 闪断需要抓 logcat / broker 日志定位。
> 机器列表 03 显示不在线，这是后端 `online` 字段，需 PC 后端确认判定逻辑。
>
> **给 PC 后端**：
> 1. P0-1 中继 token 按账号签发（`docs/40`，已发你）；
> 2. `/api/machine/list` 里 `online` 字段是怎么判定的？请给出逻辑，方便排查 03 显示不在线。
>
> **给固件任务**：
> 旧代码已作废，请以 `docs/43` 为契约基准。请先回答 §七的 6 个问题（G1 Feed Hold 最急）。
