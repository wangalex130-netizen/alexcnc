<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== 基础 ===== -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- 本地推送通知（Android 13+ / API 33+ 需运行时申请） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <!-- 外网 MJPEG 截图保存到系统相册 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- ===== 个推 SDK 必选/推荐权限（对照验证通过的测试包补齐） ===== -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.GET_TASKS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Android 12+ 注册 CID 需要精确 alarm 权限 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- vivo 角标（个推 SDK 声明为必选） -->
    <uses-permission android:name="com.vivo.notification.permission.BADGE_ICON"/>

    <!-- 个推内部广播/服务通信自定义权限 -->
    <permission
        android:name="getui.permission.GetuiService.${applicationId}"
        android:protectionLevel="signature"/>
    <uses-permission android:name="getui.permission.GetuiService.${applicationId}"/>

    <!-- tools:replace 是必须的：gtsdk 3.3.12.0 的 aar 自带 AndroidManifest，
         其 <application> 也声明了 label/icon 等属性，与我们声明的值冲突时
         Gradle 的 manifest merger 会直接构建失败（"Manifest merger failed"）。
         这里显式声明"以 App 自己的值为准"，覆盖所有可能与 gtsdk 撞车的属性，
         避免逐个属性报一轮再打一轮包（每次构建约 5 分钟）。
         测试包 alexcnc_push_test（Run 15 跑通）用的就是同样的处理。 -->
    <application android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:label="alexcnc"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:label,android:icon,android:theme,android:name,android:allowBackup,android:supportsRtl,android:usesCleartextTraffic,android:networkSecurityConfig,android:hardwareAccelerated,android:requestLegacyExternalStorage">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 个推（GeTui）推送凭据：值来自 app/build.gradle.kts 的
             manifestPlaceholders（GETUI_APPID / GETUI_APPKEY / GETUI_APPSECRET）。
             注意：未同意隐私政策前 App 只 preInit，不 initialize，满足合规红线。
             两对 key 都写，确保 SDK 一定能读到真实 AppID：
               - PUSH_APPID/KEY/SECRET：个推 SDK 真正读取的主 key
               - GETUI_APPID/KEY/SECRET：多产品/版本兜底 key（Run 10 起要求补充） -->
        <meta-data
            android:name="PUSH_APPID"
            android:value="${GETUI_APPID}" />
        <meta-data
            android:name="PUSH_APPKEY"
            android:value="${GETUI_APPKEY}" />
        <meta-data
            android:name="PUSH_APPSECRET"
            android:value="${GETUI_APPSECRET}" />
        <meta-data
            android:name="GETUI_APPID"
            android:value="${GETUI_APPID}" />
        <meta-data
            android:name="GETUI_APPKEY"
            android:value="${GETUI_APPKEY}" />
        <meta-data
            android:name="GETUI_APPSECRET"
            android:value="${GETUI_APPSECRET}" />

        <!-- flutter_local_notifications：调度通知的接收器（17.x 官方要求，
             即时通知不调度也一样注册，确保重启/覆盖安装后插件正常工作） -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+（API 30+）包可见性：个推 SDK 需显式声明才能查到自身组件，
             否则在 Android 11+ 设备上 CID 注册/透传接收会失效。 -->
        <intent>
            <action android:name="com.getui.sdk.action" />
        </intent>
    </queries>
</manifest>
